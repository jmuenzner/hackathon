// Code generated by 'banks apply'; DO NOT EDIT.

package Journeys

import (
	"context"
	"fmt"

	"github.com/autopilothq/lg"

	"github.com/autopilothq/banks/contract"
	ct "github.com/autopilothq/banks/contract/types"
	bp "github.com/autopilothq/banks/protocol"
	"github.com/autopilothq/banks/util"

	dep1 "github.com/hackathon/journeys/resources/Journeys/Legacy"
	dep0 "github.com/hackathon/journeys/resources/Journeys/protocol"
)

const (
	resourceID = 1

	msgIDGetTree = 65538

	spIDLegacy = 1
)

func init() {
	var _ util.Fuse

	contract.StoragePlan(resourceID, spIDLegacy, dep1.SetupDatascope, dep1.TeardownDatascope)

	contract.Request(msgIDGetTree, &dep0.GetTreeRequest{}, &dep0.GetTreeResponse{},
		func(ctx context.Context, request *bp.Request, log lg.Log, aux ct.Auxiliary) (res *bp.Response) {
			var dispatch func(uint32) *bp.Response
			dispatch = func(step uint32) (res *bp.Response) { // nolint: shadow
				switch step {
				case 0:

					var (
						migrationID uint32
						storageID   uint32
						exists      bool
						err         error
						ready       bool
					)

					ready, err = contract.WaitUntilNullMigrationComplete(ctx, request.DatascopeID, resourceID)
					if err != nil {
						return request.MakeFatalResponse(bp.ResponseCode_ERR_UNKNOWN,
							fmt.Sprintf("Journeys.GetTree error waiting: %s", err.Error()))
					}
					if !ready {
						return dispatch(0)
					}

					migrationID, exists, err = contract.GetActiveMigrationID(request.DatascopeID, resourceID)
					if err != nil {
						return request.MakeFatalResponse(bp.ResponseCode_ERR_UNKNOWN,
							fmt.Sprintf("Journeys.GetTree error getting active migration: %s", err.Error()))
					}

					if exists {
						return dispatch(migrationID)
					}

					storageID, exists, err = contract.GetStoragePlanID(request.DatascopeID, resourceID)
					if err != nil {
						return request.MakeFatalResponse(bp.ResponseCode_ERR_UNKNOWN,
							fmt.Sprintf("Journeys.GetTree error getting current storage plan: %s", err.Error()))
					}
					if exists {
						return dispatch(storageID)
					}

					return request.MakeFatalResponse(bp.ResponseCode_ERR_UNKNOWN,
						"Journeys.GetTree failed: no storage plan available")

				case spIDLegacy:

					contract.TraceStoragePlanImpl(log, spIDLegacy, "Legacy", msgIDGetTree, request, res)
					return dep1.GetTree(request, log, aux)

				default:
					return request.MakeFatalResponse(bp.ResponseCode_ERR_UNKNOWN,
						fmt.Sprintf("Invalid Journeys.GetTree dispatch step %d", step))
				}
			}

			return dispatch(0)
		})
}
